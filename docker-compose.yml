version: "3.8"
services:
  telegram-bot:
    container_name: steganography-telegram-bot
    build: .
    stop_signal: SIGINT
    restart: always
    tmpfs:
      - /steganography-telegram-bot/tmp
    env_file:
      - .env
